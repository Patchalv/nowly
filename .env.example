# ==============================================================================
# Nowly Environment Variables Template
# ==============================================================================
# Copy this file to .env.local and fill in the actual values
# NEVER commit .env.local to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# Next.js Configuration
# ------------------------------------------------------------------------------
# The base URL of your application (used for absolute URLs, redirects, etc.)
NEXT_PUBLIC_VERCEL_URL=http://localhost:3000

# Vercel environment (development, preview, production)
NEXT_PUBLIC_VERCEL_ENV=development

# ------------------------------------------------------------------------------
# Supabase Configuration
# ------------------------------------------------------------------------------
# Your Supabase project URL
# Find this in your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous/public key (safe to expose in client-side code)
# Find this in your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (ONLY use on server-side, NEVER expose to client)
# Required for admin operations that bypass Row Level Security
# Find this in your Supabase project settings (keep this secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here